import React from "react";
import Form from "../Components/Form";

export default function Signin() {
  return (
    <div>
      <Form title="Sign in to our platform" isLogin={false} />
    </div>
  );
}
